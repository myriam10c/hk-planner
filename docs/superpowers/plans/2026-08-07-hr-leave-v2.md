# HR v2 Implementation Plan (historique, signatures + PDF, fériés UAE)

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Vue manager avec historique complet des congés, formulaire de congé signé (signatures dessinées, PDF archivé dans Supabase Storage), et jours fériés UAE visibles dans HR + planning.

**Architecture:** Frontend vanilla-JS mono-fichiers (`hr.js` chargé avant `app.js`, scope global partagé). Backend = une seule edge function Supabase `hostaway-proxy` (service_role, gates hrAuth staff/manager/owner). PDF généré côté edge function avec pdf-lib, archivé dans le bucket privé `hr-forms`, servi par URL signée 60 s.

**Tech Stack:** Vanilla JS, Playwright (tests), Supabase (Postgres + Storage + edge function Deno), `npm:pdf-lib` côté Deno.

**Spec:** `docs/superpowers/specs/2026-08-07-hr-leave-v2-design.md`

## Global Constraints

- Repo : `/Users/hillal/Documents/Wix new/hk-planner-repo`, branche `main`. Prod est servie depuis l'arbre de travail NON commité : ne JAMAIS stasher, ne jamais « ranger » les fichiers untracked (`deno.lock`, `saas/`, etc.).
- Tout texte visible par l'équipe est en anglais. Aucun tiret cadratin « — » dans les textes produits.
- `hr.js` : helpers purs (sans DOM ni réseau) en tête de fichier, exposés sur `window` pour les tests ; état et rendu en dessous. Suivre ce découpage existant.
- Edge function : UN seul fichier `supabase/functions/hostaway-proxy/index.ts`. Routes gardées par `hrAuth(sb, req, "staff"|"manager"|"owner")`. Réponses via `jsonResp(obj, status?)`.
- Migrations : fichier SQL dans `supabase/migrations/`, appliqué via Supabase MCP `apply_migration` sur le projet prod `dqjnqvbxfwtvrjwnnmns` (voir `supabase/migrations/README.md`).
- Tests : `npx playwright test tests/hr.spec.ts --project=desktop` avec un serveur local (`python3 -m http.server 8888` + `HK_PLANNER_URL=http://localhost:8888`). Les tests exercent les helpers purs via `page.evaluate`.
- Commits fréquents, messages style existant (`feat(rh): ...`, `fix(conges): ...`). Ne commiter QUE les fichiers de la tâche.
- Ne PAS déployer (ni proxy ni front) avant la tâche 10, réservée à la session principale.

---

### Task 1: Migration SQL (colonnes signatures, table fériés, bucket)

**Files:**
- Create: `supabase/migrations/20260807120000_hr_v2.sql`

**Interfaces:**
- Produces: colonnes `leave_requests.employee_signature TEXT`, `leave_requests.manager_signature TEXT`, `leave_requests.form_path TEXT` ; table `public.public_holidays(id, holiday_date DATE UNIQUE, name TEXT)` ; bucket Storage privé `hr-forms`.

- [ ] **Step 1: Écrire la migration**

```sql
-- ============================================================================
-- HR v2 : signatures des demandes de congé, formulaire PDF archivé,
-- jours fériés UAE.
--
-- - Signatures stockées en data-URL PNG base64 (<= 100 Ko, validé par l'edge
--   function). RLS sans policy comme le reste du module RH : seul le
--   service_role de l'edge function y accède.
-- - form_path : chemin du PDF archivé dans le bucket privé hr-forms
--   (leave-forms/{id}.pdf), null tant qu'aucun PDF n'a été généré.
-- - public_holidays : liste éditable par les managers. Les dates islamiques
--   dépendent de la lune : seed « (to confirm) » à ajuster à l'annonce.
-- ============================================================================

ALTER TABLE public.leave_requests
  ADD COLUMN IF NOT EXISTS employee_signature TEXT,
  ADD COLUMN IF NOT EXISTS manager_signature  TEXT,
  ADD COLUMN IF NOT EXISTS form_path          TEXT;

COMMENT ON COLUMN public.leave_requests.employee_signature IS
  'Signature dessinée par l''employé à la soumission (data:image/png;base64). Null si saisie par un manager pour un tiers ou antérieure à la feature.';
COMMENT ON COLUMN public.leave_requests.manager_signature IS
  'Signature dessinée par le manager à l''approbation. Null si rejet ou antérieure à la feature.';
COMMENT ON COLUMN public.leave_requests.form_path IS
  'Chemin du PDF archivé dans le bucket hr-forms, ex. leave-forms/42.pdf.';

CREATE TABLE IF NOT EXISTS public.public_holidays (
  id           BIGINT GENERATED ALWAYS AS IDENTITY PRIMARY KEY,
  holiday_date DATE NOT NULL UNIQUE,
  name         TEXT NOT NULL,
  created_at   TIMESTAMPTZ NOT NULL DEFAULT now()
);

ALTER TABLE public.public_holidays ENABLE ROW LEVEL SECURITY;

COMMENT ON TABLE public.public_holidays IS
  'Jours fériés UAE, éditables par les managers via l''edge function. Aucun impact sur le décompte des congés.';

INSERT INTO public.public_holidays (holiday_date, name) VALUES
  ('2026-08-25', 'Prophet''s Birthday (to confirm)'),
  ('2026-12-02', 'Eid Al Etihad / National Day'),
  ('2026-12-03', 'Eid Al Etihad / National Day'),
  ('2027-01-01', 'New Year''s Day'),
  ('2027-03-10', 'Eid Al Fitr (to confirm)'),
  ('2027-03-11', 'Eid Al Fitr holiday (to confirm)'),
  ('2027-03-12', 'Eid Al Fitr holiday (to confirm)'),
  ('2027-05-16', 'Arafat Day (to confirm)'),
  ('2027-05-17', 'Eid Al Adha (to confirm)'),
  ('2027-05-18', 'Eid Al Adha holiday (to confirm)'),
  ('2027-05-19', 'Eid Al Adha holiday (to confirm)'),
  ('2027-06-06', 'Islamic New Year (to confirm)'),
  ('2027-08-14', 'Prophet''s Birthday (to confirm)'),
  ('2027-12-02', 'Eid Al Etihad / National Day'),
  ('2027-12-03', 'Eid Al Etihad / National Day')
ON CONFLICT (holiday_date) DO NOTHING;

INSERT INTO storage.buckets (id, name, public)
VALUES ('hr-forms', 'hr-forms', false)
ON CONFLICT (id) DO NOTHING;
```

- [ ] **Step 2: Appliquer la migration en prod**

Via Supabase MCP `apply_migration` sur le projet `dqjnqvbxfwtvrjwnnmns`, nom `hr_v2`, avec le SQL ci-dessus. Si le MCP n'est pas disponible dans la session du subagent, STOP : remonter à la session principale qui l'appliquera.

- [ ] **Step 3: Vérifier**

Via MCP `run_sql` (ou SQL editor) :
```sql
SELECT column_name FROM information_schema.columns
 WHERE table_name = 'leave_requests'
   AND column_name IN ('employee_signature','manager_signature','form_path');
SELECT count(*) FROM public.public_holidays;
SELECT id, public FROM storage.buckets WHERE id = 'hr-forms';
```
Attendu : 3 colonnes, count >= 15, bucket `hr-forms` avec `public = false`.

- [ ] **Step 4: Commit**

```bash
git add supabase/migrations/20260807120000_hr_v2.sql
git commit -m "feat(rh): migration v2 (signatures, form_path, feries UAE, bucket hr-forms)"
```

---

### Task 2: Helpers purs hr.js + tests (historique, fériés)

**Files:**
- Modify: `hr.js` (section helpers purs, avant la ligne `window.HR_LEAVE_TYPES = HR_LEAVE_TYPES;`)
- Test: `tests/hr.spec.ts` (ajouter en fin de fichier)

**Interfaces:**
- Produces: `hrSplitHistory(rows, cleanerId, today)` renvoie `{ active: row[], past: row[] }` ; `hrUpcomingHolidays(rows, today, limit)` renvoie `row[]` triés par date croissante ; `hrHolidayNameOn(rows, day)` renvoie `string|null`. Les trois exposées sur `window`.
- Consumes: rien (helpers purs).

- [ ] **Step 1: Écrire les tests qui échouent**

Ajouter à la fin de `tests/hr.spec.ts` :

```ts
test('hrSplitHistory separe en-cours/a-venir du passe, tous statuts', async ({ page }) => {
  const r = await page.evaluate(() => {
    const w = window as any;
    const rows = [
      { id: 1, cleaner_id: 5, status: 'approved',  start_date: '2026-07-01', end_date: '2026-07-05' },
      { id: 2, cleaner_id: 5, status: 'approved',  start_date: '2026-08-10', end_date: '2026-08-12' },
      { id: 3, cleaner_id: 5, status: 'pending',   start_date: '2026-09-01', end_date: '2026-09-03' },
      { id: 4, cleaner_id: 5, status: 'rejected',  start_date: '2026-08-20', end_date: '2026-08-22' },
      { id: 5, cleaner_id: 5, status: 'cancelled', start_date: '2026-06-01', end_date: '2026-06-02' },
      { id: 6, cleaner_id: 9, status: 'approved',  start_date: '2026-08-10', end_date: '2026-08-12' },
      { id: 7, cleaner_id: 5, status: 'approved',  start_date: '2026-08-01', end_date: '2026-08-09' },
    ];
    const s = w.hrSplitHistory(rows, 5, '2026-08-07');
    return { active: s.active.map((x: any) => x.id), past: s.past.map((x: any) => x.id) };
  });
  // Actifs : pending/approved dont end_date >= today, tri start croissant.
  expect(r.active).toEqual([7, 2, 3]);
  // Passé/terminé : le reste (approuvé passé, rejeté, annulé), tri start décroissant.
  expect(r.past).toEqual([4, 1, 5]);
});

test('hrUpcomingHolidays filtre et trie, hrHolidayNameOn retrouve un ferie', async ({ page }) => {
  const r = await page.evaluate(() => {
    const w = window as any;
    const rows = [
      { holiday_date: '2026-12-02', name: 'Eid Al Etihad / National Day' },
      { holiday_date: '2026-08-25', name: "Prophet's Birthday (to confirm)" },
      { holiday_date: '2026-01-01', name: 'New Year 2026' },
    ];
    return {
      upcoming: w.hrUpcomingHolidays(rows, '2026-08-07', 10).map((h: any) => h.holiday_date),
      capped: w.hrUpcomingHolidays(rows, '2026-08-07', 1).map((h: any) => h.holiday_date),
      hit: w.hrHolidayNameOn(rows, '2026-12-02'),
      miss: w.hrHolidayNameOn(rows, '2026-12-04'),
      empty: w.hrUpcomingHolidays(null, '2026-08-07', 5),
    };
  });
  expect(r.upcoming).toEqual(['2026-08-25', '2026-12-02']);
  expect(r.capped).toEqual(['2026-08-25']);
  expect(r.hit).toBe('Eid Al Etihad / National Day');
  expect(r.miss).toBeNull();
  expect(r.empty).toEqual([]);
});
```

- [ ] **Step 2: Vérifier qu'ils échouent**

```bash
cd "/Users/hillal/Documents/Wix new/hk-planner-repo"
python3 -m http.server 8888 &  # noter le PID, kill à la fin
HK_PLANNER_URL=http://localhost:8888 npx playwright test tests/hr.spec.ts --project=desktop
```
Attendu : les 2 nouveaux tests FAIL (`hrSplitHistory is not a function`), les anciens passent.

- [ ] **Step 3: Implémenter les helpers**

Dans `hr.js`, juste AVANT `window.HR_LEAVE_TYPES = HR_LEAVE_TYPES;` :

```js
// Découpe l'historique d'un employé : demandes vivantes (pending/approved non
// terminées, tri chronologique) puis le reste (passé, rejeté, annulé, tri
// antichronologique). Piloté par la vue "Leave history" du dossier manager.
function hrSplitHistory(rows, cleanerId, today){
  const cid = Number(cleanerId);
  const mine = (Array.isArray(rows) ? rows : []).filter(r => Number(r.cleaner_id) === cid);
  const isActive = r => (r.status === 'pending' || r.status === 'approved') && r.end_date >= today;
  const active = mine.filter(isActive).sort((a, b) => (a.start_date < b.start_date ? -1 : 1));
  const past = mine.filter(r => !isActive(r)).sort((a, b) => (a.start_date < b.start_date ? 1 : -1));
  return { active: active, past: past };
}

// Fériés à venir (>= today), tri croissant, plafonnés à limit.
function hrUpcomingHolidays(rows, today, limit){
  return (Array.isArray(rows) ? rows : [])
    .filter(h => h && h.holiday_date >= today)
    .sort((a, b) => (a.holiday_date < b.holiday_date ? -1 : 1))
    .slice(0, limit || 10);
}

// Nom du férié tombant ce jour-là, null sinon.
function hrHolidayNameOn(rows, day){
  const h = (Array.isArray(rows) ? rows : []).find(x => x && x.holiday_date === day);
  return h ? h.name : null;
}
```

Puis, à côté de `window.HR_LEAVE_TYPES = HR_LEAVE_TYPES;`, ajouter :

```js
window.hrSplitHistory = hrSplitHistory;
window.hrUpcomingHolidays = hrUpcomingHolidays;
window.hrHolidayNameOn = hrHolidayNameOn;
```

(Note : `leaveDays` etc. sont des déclarations `function` top-level donc déjà sur window ; ces trois nouvelles le seraient aussi, mais l'export explicite suit le pattern du fichier et protège si le fichier passe un jour en module.)

- [ ] **Step 4: Vérifier que les tests passent**

```bash
HK_PLANNER_URL=http://localhost:8888 npx playwright test tests/hr.spec.ts --project=desktop
```
Attendu : tout PASS. Tuer le serveur http (`kill <PID>`).

- [ ] **Step 5: Commit**

```bash
git add hr.js tests/hr.spec.ts
git commit -m "feat(rh): helpers purs historique conges et feries UAE"
```

---

### Task 3: Backend, historique + fériés (lecture et CRUD)

**Files:**
- Modify: `supabase/functions/hostaway-proxy/index.ts`

**Interfaces:**
- Consumes: table `public_holidays` (Task 1), `hrAuth`, `jsonResp`, `HR_TODAY`.
- Produces: `hrOverview` renvoie en plus `history` (row[] leave_requests, 200 dernières, tous statuts, tri start_date desc) et `holidays` (row[] public_holidays `id, holiday_date, name`, depuis J-30, tri croissant) ; `hrMyLeave` renvoie en plus `holidays` (même forme) ; `getAllData` renvoie en plus `holidays` ; routes POST `hrSaveHoliday` (`{holiday_date, name}`, manager) et `hrDeleteHoliday` (`{id}`, manager).

- [ ] **Step 1: Enregistrer les nouvelles routes**

Dans la table des routes (vers la ligne 740, à côté de `["hrDecideLeave", "POST"]`), ajouter :

```ts
  ["hrSaveHoliday", "POST"],
  ["hrDeleteHoliday", "POST"],
  ["hrLeaveForm", "GET"],
```

(`hrLeaveForm` est implémentée en Task 5 mais on déclare la route ici pour ne toucher cette table qu'une fois. Vérifier d'abord comment cette table est utilisée : suivre le pattern des entrées hr* existantes.)

- [ ] **Step 2: Étendre hrOverview**

Dans le bloc `if (action === "hrOverview")`, ajouter deux requêtes au `Promise.all` existant (après `docRes`) :

```ts
        sb.from("leave_requests").select("*").order("start_date", { ascending: false }).limit(200),
        sb.from("public_holidays").select("id, holiday_date, name")
          .gte("holiday_date", new Date(Date.now() - 30 * 86400000).toISOString().slice(0, 10))
          .order("holiday_date"),
```

Nommer les résultats `historyRes` et `holidayRes` dans la destructuration, et ajouter au `jsonResp` final :

```ts
        history: historyRes.data || [],
        holidays: holidayRes.data || [],
```

- [ ] **Step 3: Étendre hrMyLeave et getAllData**

Dans `hrMyLeave`, ajouter la même requête fériés au `Promise.all` (`holidayRes`) et `holidays: holidayRes.data || []` à la réponse.

Dans `getAllData` (bloc `Promise.all` vers la ligne 2096), ajouter en dernière position :

```ts
        sb.from("public_holidays").select("holiday_date, name")
          .gte("holiday_date", new Date(Date.now() - 7 * 86400000).toISOString().split('T')[0])
          .lte("holiday_date", new Date(Date.now() + 120 * 86400000).toISOString().split('T')[0])
          .order("holiday_date"),
```

Nommer `holidayRes` dans la destructuration et ajouter `holidays: holidayRes.data || [],` à la réponse (à côté de `leaves:`). Pas d'id : le planning n'en a pas besoin, et ce payload part aussi aux cleaners.

- [ ] **Step 4: Implémenter hrSaveHoliday / hrDeleteHoliday**

Après le bloc `hrCancelLeave`, ajouter :

```ts
    if (action === "hrSaveHoliday" && req.method === "POST") {
      const g = await hrAuth(sb, req, "manager");
      if (g.err) return g.err;
      const body = await req.json();
      const date = String(body.holiday_date || "");
      const name = String(body.name || "").trim().slice(0, 120);
      if (!/^\d{4}-\d{2}-\d{2}$/.test(date)) return jsonResp({ error: "invalid holiday_date" }, 400);
      if (!name) return jsonResp({ error: "name required" }, 400);
      const { data, error } = await sb.from("public_holidays")
        .upsert({ holiday_date: date, name }, { onConflict: "holiday_date" })
        .select().single();
      if (error) return jsonResp({ error: error.message }, 500);
      return jsonResp({ status: "success", holiday: data });
    }

    if (action === "hrDeleteHoliday" && req.method === "POST") {
      const g = await hrAuth(sb, req, "manager");
      if (g.err) return g.err;
      const id = Number((await req.json()).id);
      if (!id) return jsonResp({ error: "id required" }, 400);
      const { error } = await sb.from("public_holidays").delete().eq("id", id);
      if (error) return jsonResp({ error: error.message }, 500);
      return jsonResp({ status: "success" });
    }
```

- [ ] **Step 5: Vérifier le typage**

```bash
cd "/Users/hillal/Documents/Wix new/hk-planner-repo"
npx tsc --noEmit --allowJs false --target es2022 --module esnext --moduleResolution bundler --skipLibCheck supabase/functions/hostaway-proxy/index.ts 2>&1 | head -20
```
S'il existe déjà une commande de check dans le repo (grep `tsc` dans package.json/scripts), l'utiliser à la place. Des erreurs préexistantes hors des lignes touchées sont tolérées ; aucune nouvelle erreur sur les blocs ajoutés.

- [ ] **Step 6: Commit**

```bash
git add supabase/functions/hostaway-proxy/index.ts
git commit -m "feat(rh): historique complet + feries UAE dans les payloads, CRUD feries manager"
```

---

### Task 4: Backend, signatures à la soumission et à la décision

**Files:**
- Modify: `supabase/functions/hostaway-proxy/index.ts`

**Interfaces:**
- Consumes: colonnes signatures (Task 1).
- Produces: helper `hrValidSignature(s: unknown): string | null` ; `hrSubmitLeave` accepte `employee_signature` (OBLIGATOIRE si le demandeur soumet pour lui-même, y compris un manager pour lui-même ; absente sinon) ; `hrDecideLeave` exige `manager_signature` quand `decision === "approved"`.

- [ ] **Step 1: Helper de validation**

À côté de `hrLeaveDays` (vers la ligne 368) :

```ts
// Signature dessinée : uniquement un data-URL PNG base64, 100 Ko max.
// Tout le reste est refusé (pas de SVG, pas d'URL externe, pas de JPEG).
const HR_SIG_PREFIX = "data:image/png;base64,";
function hrValidSignature(s: unknown): string | null {
  if (typeof s !== "string" || !s.startsWith(HR_SIG_PREFIX)) return null;
  if (s.length > 100_000) return null;
  if (!/^[A-Za-z0-9+/]+=*$/.test(s.slice(HR_SIG_PREFIX.length))) return null;
  return s;
}
```

- [ ] **Step 2: hrSubmitLeave**

Dans le bloc `hrSubmitLeave`, après le calcul de `target` :

```ts
      const selfSubmit = target === g.me!.cleaner_id;
      const employeeSignature = hrValidSignature(body.employee_signature);
      if (selfSubmit && !employeeSignature) {
        return jsonResp({ error: "signature required" }, 400);
      }
```

Et dans l'`insert`, ajouter :

```ts
        employee_signature: selfSubmit ? employeeSignature : null,
```

- [ ] **Step 3: hrDecideLeave**

Dans le bloc `hrDecideLeave`, après la validation de `decision` :

```ts
      const managerSignature = decision === "approved" ? hrValidSignature(body.manager_signature) : null;
      if (decision === "approved" && !managerSignature) {
        return jsonResp({ error: "manager signature required" }, 400);
      }
```

Et dans l'`update`, ajouter `manager_signature: managerSignature,`.

- [ ] **Step 4: Vérifier le typage** (même commande que Task 3 Step 5)

- [ ] **Step 5: Commit**

```bash
git add supabase/functions/hostaway-proxy/index.ts
git commit -m "feat(rh): signatures PNG validees a la soumission et a l'approbation"
```

---

### Task 5: Backend, PDF pdf-lib + archivage Storage + route hrLeaveForm

**Files:**
- Modify: `supabase/functions/hostaway-proxy/index.ts`

**Interfaces:**
- Consumes: `hrValidSignature` (Task 4), colonnes Task 1, `HR_LEAVE_LABELS`, `HR_EMPLOYEE_PUBLIC_COLS`, bucket `hr-forms`.
- Produces: `hrBuildLeaveFormPdf(sb, lr): Promise<Uint8Array>` ; `hrArchiveLeaveForm(sb, lr): Promise<string | null>` (upload + update form_path, best-effort, null si échec) ; route GET `hrLeaveForm?id=N` renvoyant `{ status, url }` (URL signée 60 s), génération paresseuse si `form_path` absent. Appels d'archivage branchés dans `hrSubmitLeave` et `hrDecideLeave`.

- [ ] **Step 1: Import pdf-lib**

En tête de fichier, après l'import supabase-js :

```ts
import { PDFDocument, StandardFonts, rgb } from "npm:pdf-lib@1.17.1";
```

- [ ] **Step 2: Génération du PDF**

Après `hrNotifyCleaner`, ajouter :

```ts
// ===========================================================================
// Formulaire de congé PDF (A4). Régénérable à tout moment depuis la base :
// le fichier archivé dans hr-forms n'est qu'une copie de commodité.
// ===========================================================================
async function hrBuildLeaveFormPdf(sb: any, lr: any): Promise<Uint8Array> {
  const [{ data: who }, { data: emp }] = await Promise.all([
    sb.from("cleaners").select("name").eq("id", lr.cleaner_id).maybeSingle(),
    sb.from("employees").select(HR_EMPLOYEE_PUBLIC_COLS).eq("cleaner_id", lr.cleaner_id).maybeSingle(),
  ]);
  const name = (who && who.name) || `#${lr.cleaner_id}`;
  const pdf = await PDFDocument.create();
  const page = pdf.addPage([595, 842]);
  const font = await pdf.embedFont(StandardFonts.Helvetica);
  const bold = await pdf.embedFont(StandardFonts.HelveticaBold);
  const ink = rgb(0.12, 0.12, 0.12);
  const grey = rgb(0.45, 0.45, 0.45);
  let y = 780;
  const text = (s: string, x: number, size: number, f = font, c = ink) =>
    page.drawText(String(s), { x, y, size, font: f, color: c });
  const row = (label: string, value: string) => {
    text(label, 60, 10, bold, grey); text(String(value || "-"), 220, 10);
    y -= 22;
  };

  text("Hillal Medini Vacation Homes Rental LLC", 60, 14, bold); y -= 20;
  text("Leave Application Form", 60, 18, bold); y -= 14;
  page.drawLine({ start: { x: 60, y }, end: { x: 535, y }, thickness: 1, color: grey });
  y -= 26;

  row("Request #", String(lr.id));
  row("Employee", name);
  row("Job title", (emp && emp.job_title) || "-");
  row("Hire date", (emp && emp.hire_date) || "-");
  row("Nationality", (emp && emp.nationality) || "-");
  y -= 8;
  row("Leave type", HR_LEAVE_LABELS[lr.leave_type] || lr.leave_type);
  row("From", lr.start_date);
  row("To", lr.end_date);
  row("Days (calendar)", String(lr.days));
  row("Reason", lr.reason || "-");
  y -= 8;
  row("Status", String(lr.status).toUpperCase());
  row("Requested by", lr.requested_by || name);
  row("Requested at", String(lr.requested_at || "").slice(0, 16).replace("T", " "));
  if (lr.decided_by) {
    row("Decided by", lr.decided_by);
    row("Decided at", String(lr.decided_at || "").slice(0, 16).replace("T", " "));
    if (lr.decision_note) row("Decision note", lr.decision_note);
  }

  // Blocs signatures côte à côte.
  y -= 30;
  const sigBlock = async (x: number, title: string, sig: string | null, fallback: string) => {
    page.drawText(title, { x, y, size: 10, font: bold, color: grey });
    page.drawRect({ x, y: y - 84, width: 210, height: 74, borderColor: grey, borderWidth: 0.8 });
    if (sig) {
      try {
        const png = await pdf.embedPng(sig);
        const dims = png.scaleToFit(190, 58);
        page.drawImage(png, { x: x + 10, y: y - 76, width: dims.width, height: dims.height });
        return;
      } catch (_e) { /* signature illisible : tomber sur le fallback */ }
    }
    page.drawText(fallback, { x: x + 10, y: y - 48, size: 8, font, color: grey, maxWidth: 190, lineHeight: 10 });
  };
  const empFallback = lr.employee_signature ? "" :
    (lr.requested_by && lr.requested_by !== name
      ? `Recorded by ${lr.requested_by} on behalf of employee`
      : `Submitted in app by ${lr.requested_by || name} on ${String(lr.requested_at || "").slice(0, 10)}`);
  const mgrFallback = lr.manager_signature ? "" :
    (lr.decided_by ? `${String(lr.status)} in app by ${lr.decided_by} on ${String(lr.decided_at || "").slice(0, 10)}` : "Pending decision");
  await sigBlock(60, "Employee signature", lr.employee_signature || null, empFallback);
  await sigBlock(325, "Manager signature", lr.manager_signature || null, mgrFallback);
  y -= 110;
  page.drawText(`Generated by HK Planner on ${new Date().toISOString().slice(0, 16).replace("T", " ")} UTC`,
    { x: 60, y, size: 8, font, color: grey });
  return await pdf.save();
}

// Archive (ou re-archive) le PDF d'une demande. Best-effort : ne lève jamais,
// renvoie le chemin ou null. Le PDF se régénérera au premier téléchargement.
async function hrArchiveLeaveForm(sb: any, lr: any): Promise<string | null> {
  try {
    const bytes = await hrBuildLeaveFormPdf(sb, lr);
    const path = `leave-forms/${lr.id}.pdf`;
    const { error } = await sb.storage.from("hr-forms")
      .upload(path, bytes, { contentType: "application/pdf", upsert: true });
    if (error) throw error;
    await sb.from("leave_requests").update({ form_path: path }).eq("id", lr.id);
    return path;
  } catch (e) {
    console.warn("[hr] leave form archive failed", e);
    return null;
  }
}
```

- [ ] **Step 3: Brancher l'archivage**

Dans `hrSubmitLeave`, juste après l'insert réussi (`if (error) return ...` passé), ajouter :

```ts
      const archivePromise = hrArchiveLeaveForm(sb, data);
      try { (globalThis as any).EdgeRuntime?.waitUntil?.(archivePromise); } catch (_e) { /* best effort */ }
```

Dans `hrDecideLeave`, juste après l'update réussi (`if (!data) return ...` passé), la même chose avec `data`.

(Pattern `waitUntil` identique à `alertsPromise` dans `hrOverview` : la réponse HTTP ne doit pas attendre la génération.)

- [ ] **Step 4: Route hrLeaveForm**

Après le bloc `hrCancelLeave` (regarder comment les routes GET existantes lisent leurs params, ex. `hrGetCompensation` avec `cleaner_id` ; utiliser le même mécanisme) :

```ts
    if (action === "hrLeaveForm") {
      const g = await hrAuth(sb, req, "staff");
      if (g.err) return g.err;
      const id = Number(url.searchParams.get("id"));
      if (!id) return jsonResp({ error: "id required" }, 400);
      const { data: lr } = await sb.from("leave_requests").select("*").eq("id", id).maybeSingle();
      if (!lr) return jsonResp({ error: "request not found" }, 404);
      if (g.me!.role !== "manager" && lr.cleaner_id !== g.me!.cleaner_id) {
        return jsonResp({ error: "not your request" }, 403);
      }
      let path = lr.form_path as string | null;
      if (!path) path = await hrArchiveLeaveForm(sb, lr);
      if (!path) return jsonResp({ error: "failed to build the form, try again" }, 500);
      const { data: signed, error } = await sb.storage.from("hr-forms").createSignedUrl(path, 60);
      if (error || !signed?.signedUrl) return jsonResp({ error: "failed to sign the download link" }, 500);
      return jsonResp({ status: "success", url: signed.signedUrl });
    }
```

(NB : si `url.searchParams` n'est pas déjà disponible dans ce scope, reprendre exactement la variable utilisée par les autres routes GET pour lire `action`.)

- [ ] **Step 5: Vérifier le typage** (même commande que Task 3 Step 5)

- [ ] **Step 6: Commit**

```bash
git add supabase/functions/hostaway-proxy/index.ts
git commit -m "feat(rh): formulaire de conge PDF archive dans hr-forms + route hrLeaveForm"
```

---

### Task 6: Frontend, pad de signature réutilisable

**Files:**
- Modify: `hr.js` (section état/rendu, après `hrVal`)

**Interfaces:**
- Consumes: `esc()`, `toast()` (globales app.js), div `#modal` de `index.html` (pattern `closeModal` d'app.js:1796).
- Produces: `hrOpenSignaturePad(title, onDone)` : ouvre une modale canvas ; `onDone(dataUrlPng)` appelé sur Confirm ; Cancel ferme sans callback. Handlers `hrSigClear`, `hrSigCancel`, `hrSigConfirm` (déclarations `function` top-level, atteignables par la délégation `data-action`).

- [ ] **Step 1: Implémenter le pad**

Ajouter dans `hr.js`, après la définition de `hrVal` :

```js
// ===========================================================================
// Pad de signature. Modale canvas dans #modal (meme hote que closeModal).
// Les traits sont captures en pointer events ; touch-action:none empeche le
// scroll de la page pendant qu'on dessine sur mobile.
// ===========================================================================
let hrSigOnDone = null;
let hrSigDrawn = false;

function hrOpenSignaturePad(title, onDone){
  hrSigOnDone = onDone;
  hrSigDrawn = false;
  document.getElementById('modal').innerHTML =
    '<div class="modal-overlay"><div class="hr-card" style="max-width:380px;margin:auto;padding:16px;border-radius:12px;background:var(--bg2,#fff)">' +
    '<div class="hr-name" style="margin-bottom:4px">' + esc(title) + '</div>' +
    '<div class="hr-meta" style="margin-bottom:8px">Draw your signature below with your finger.</div>' +
    '<canvas id="hrSigCanvas" width="640" height="280" style="background:#fff;border:1px dashed #999;border-radius:8px;touch-action:none;width:100%;display:block"></canvas>' +
    '<div class="hr-actions" style="margin-top:10px">' +
    '<button class="hr-btn-alt" data-action="hrSigClear">Clear</button>' +
    '<button class="hr-btn-alt" data-action="hrSigCancel">Cancel</button>' +
    '<button class="hr-btn-ok" data-action="hrSigConfirm">Confirm</button>' +
    '</div></div></div>';
  hrSigBind();
}

function hrSigBind(){
  const c = document.getElementById('hrSigCanvas');
  const ctx = c.getContext('2d');
  ctx.fillStyle = '#fff'; ctx.fillRect(0, 0, c.width, c.height);
  ctx.strokeStyle = '#14213d'; ctx.lineWidth = 3; ctx.lineCap = 'round'; ctx.lineJoin = 'round';
  let drawing = false;
  const pos = (e) => {
    const r = c.getBoundingClientRect();
    return { x: (e.clientX - r.left) * (c.width / r.width), y: (e.clientY - r.top) * (c.height / r.height) };
  };
  c.addEventListener('pointerdown', (e) => {
    e.preventDefault(); drawing = true; hrSigDrawn = true;
    c.setPointerCapture(e.pointerId);
    const p = pos(e); ctx.beginPath(); ctx.moveTo(p.x, p.y); ctx.lineTo(p.x + 0.1, p.y + 0.1); ctx.stroke();
  });
  c.addEventListener('pointermove', (e) => {
    if (!drawing) return; e.preventDefault();
    const p = pos(e); ctx.lineTo(p.x, p.y); ctx.stroke();
  });
  const stop = () => { drawing = false; };
  c.addEventListener('pointerup', stop);
  c.addEventListener('pointercancel', stop);
}

function hrSigClear(){ hrSigDrawn = false; hrSigBind(); }
function hrSigCancel(){ hrSigOnDone = null; document.getElementById('modal').innerHTML = ''; }
function hrSigConfirm(){
  if (!hrSigDrawn) { toast('Please sign first', 'error'); return; }
  const c = document.getElementById('hrSigCanvas');
  const data = c.toDataURL('image/png');
  const cb = hrSigOnDone;
  hrSigCancel();
  if (cb) cb(data);
}
```

- [ ] **Step 2: Vérifier la délégation data-action**

Chercher dans `app.js` le listener global de clic (`data-action`) et confirmer qu'il résout `hrSigClear`/`hrSigCancel`/`hrSigConfirm` (fonctions top-level = propriétés de window en script classique). Si la délégation utilise une table blanche d'actions, y ajouter les trois noms.

- [ ] **Step 3: Test manuel rapide**

```bash
python3 -m http.server 8888
```
Dans un navigateur (ou Playwright headed), console : `hrOpenSignaturePad('Test', d => console.log(d.slice(0, 40)))`. Dessiner, Confirm : la console affiche `data:image/png;base64,...`. Clear vide le canvas ; Confirm sans dessin affiche le toast d'erreur.

- [ ] **Step 4: Vérifier que les tests existants passent toujours**

```bash
HK_PLANNER_URL=http://localhost:8888 npx playwright test tests/hr.spec.ts --project=desktop
```

- [ ] **Step 5: Commit**

```bash
git add hr.js
git commit -m "feat(rh): pad de signature canvas reutilisable"
```

---

### Task 7: Frontend, signatures branchées sur soumission et approbation

**Files:**
- Modify: `hr.js` (`hrSubmitMine`, `hrDecide`, carte pending côté manager)

**Interfaces:**
- Consumes: `hrOpenSignaturePad` (Task 6), routes Task 4.
- Produces: `hrSubmitMine()` ouvre le pad puis envoie `employee_signature` ; `hrDecide(id, decision)` ouvre le pad si `approved` puis envoie `manager_signature` ; nouvelle fonction interne `hrSendDecision(id, decision, signature)`.

- [ ] **Step 1: hrSubmitMine avec signature**

Remplacer le corps de `hrSubmitMine` : après le `confirm(...)` existant, au lieu d'appeler `apiWrite` directement, ouvrir le pad :

```js
async function hrSubmitMine(){
  if (hrSubmitting) return;
  const start = hrVal('hrMineStart'), end = hrVal('hrMineEnd');
  if (!start || !end) { toast('Pick both dates', 'error'); return; }
  const d = leaveDays(start, end);
  if (!d) { toast('End date must be on or after start date', 'error'); return; }
  if (!confirm('Request ' + d + ' day' + (d > 1 ? 's' : '') + ' off, from ' + start + ' to ' + end + '?')) return;
  const leaveType = hrVal('hrMineType') || 'annual';
  const reason = hrVal('hrMineReason') || null;
  hrOpenSignaturePad('Sign your leave request', async (signature) => {
    if (hrSubmitting) return;
    hrSubmitting = true;
    try {
      await apiWrite('hrSubmitLeave', { body: {
        leave_type: leaveType, start_date: start, end_date: end,
        reason: reason, employee_signature: signature,
      }});
      toast('Request sent to your manager', 'success');
      hrData = null;
      await loadHR();
    } catch (e) {
      toast((e && e.message) || 'Failed to send', 'error');
    } finally {
      hrSubmitting = false;
    }
  });
}
```

(Les valeurs des champs sont lues AVANT d'ouvrir le pad : la modale ne détruit pas le formulaire, mais on ne dépend pas de ça.)

- [ ] **Step 2: hrDecide avec signature manager**

Remplacer `hrDecide` par :

```js
// Approve exige la signature du manager (pad) ; reject part directement.
function hrDecide(id, decision){
  if (decision === 'approved') {
    hrOpenSignaturePad('Manager signature to approve', (signature) => hrSendDecision(id, 'approved', signature));
  } else {
    hrSendDecision(id, decision, null);
  }
}

async function hrSendDecision(id, decision, signature){
  if (hrSubmitting) return;
  hrSubmitting = true;
  try {
    await apiWrite('hrDecideLeave', { body: { id: id, decision: decision, manager_signature: signature } });
    toast('Leave ' + decision, decision === 'approved' ? 'success' : 'info');
    hrData = null;
    await loadHR();
  } catch (e) {
    toast((e && e.message) || 'Failed', 'error');
  } finally {
    hrSubmitting = false;
  }
}
```

- [ ] **Step 3: Note « Book leave for this person »**

`hrSubmitLeaveFor` reste SANS signature (le serveur l'accepte : soumission manager pour un tiers). Ne pas le modifier.

- [ ] **Step 4: Tests + vérification manuelle**

Relancer `tests/hr.spec.ts` (tout doit passer). Test manuel local impossible sans PIN/session : la vérification de bout en bout se fait en Task 10.

- [ ] **Step 5: Commit**

```bash
git add hr.js
git commit -m "feat(rh): signature employe a la soumission, signature manager a l'approbation"
```

---

### Task 8: Frontend, Leave history + bouton Download form

**Files:**
- Modify: `hr.js` (`renderHRDetail`, `hrRequestCard`, `renderHRMine` ; nouvelle fonction `hrDownloadForm`)

**Interfaces:**
- Consumes: `hrData.history` (Task 3), `hrSplitHistory` (Task 2), route `hrLeaveForm` (Task 5), `api()`.
- Produces: section « Leave history » dans le dossier employé ; bouton `data-action="hrDownloadForm" data-arg0="{id}"` sur les cartes (statuts pending/approved/rejected, pas cancelled) côté manager ET côté employé ; `hrDownloadForm(id)` ouvre l'URL signée dans un nouvel onglet.

- [ ] **Step 1: hrDownloadForm**

```js
// Telecharge le formulaire PDF d'une demande via une URL signee courte duree.
let hrFormLoading = false;
async function hrDownloadForm(id){
  if (hrFormLoading) return;
  hrFormLoading = true;
  try {
    const r = await api('hrLeaveForm', { params: { id: Number(id) } });
    if (r && r.error) throw new Error(r.error);
    if (!r || !r.url) throw new Error('No download link returned');
    window.open(r.url, '_blank');
  } catch (e) {
    toast((e && e.message) || 'Failed to download the form', 'error');
  } finally {
    hrFormLoading = false;
  }
}
```

- [ ] **Step 2: Bouton sur les cartes**

Dans `hrRequestCard(r, withActions)`, ajouter après le badge de statut (avant le `</div>` fermant `.hr-row`) rien, et après le bloc `withActions`, ajouter :

```js
    (r.status !== 'cancelled'
      ? '<div class="hr-actions" style="margin-top:6px"><button class="hr-btn-alt" data-action="hrDownloadForm" data-arg0="' + r.id + '">Download form</button></div>'
      : '') +
```

(Concrètement : le return de `hrRequestCard` se termine par `... (withActions ? ... : '') + NOUVEAU_BLOC + '</div>';`.)

- [ ] **Step 3: Leave history dans le dossier employé**

Dans `renderHRDetail`, remplacer le calcul `history` (lignes ~450-452) et la section « Current and upcoming leave » par :

```js
  const split = hrSplitHistory(hrData.history || [], cid, hrToday());
```

et la section :

```js
    h += '<div class="hr-section"><h3>Leave history</h3>';
    const renderRow = (r) => hrRequestCard(r, false) +
      ((r.status === 'pending' || r.status === 'approved') && r.end_date >= hrToday()
        ? '<div class="hr-actions" style="margin-top:-4px;margin-bottom:8px"><button class="hr-btn-alt" data-action="hrCancelRequest" data-arg0="' + r.id + '">Cancel this leave</button></div>'
        : '');
    if (!split.active.length && !split.past.length) {
      h += '<div class="hr-empty">No leave request yet.</div>';
    } else {
      h += split.active.map(renderRow).join('');
      if (split.past.length) {
        h += '<div class="hr-meta" style="margin:10px 0 6px;font-weight:700">Past and closed</div>';
        h += split.past.map(renderRow).join('');
      }
    }
    h += '</div>';
```

Fallback : si `hrData.history` est absent (vieux payload en cache), `hrSplitHistory` reçoit `[]` et la section affiche « No leave request yet. » ; acceptable, un refresh recharge.

- [ ] **Step 4: Cartes de `renderHRMine`**

Rien à changer : `hrRequestCard` porte maintenant le bouton Download pour les demandes de l'employé aussi (le serveur limite déjà l'accès à ses propres formulaires).

- [ ] **Step 5: Tests**

Relancer `tests/hr.spec.ts` : tout passe (les helpers purs n'ont pas bougé).

- [ ] **Step 6: Commit**

```bash
git add hr.js
git commit -m "feat(rh): historique complet des conges cote manager + telechargement du formulaire"
```

---

### Task 9: Frontend, fériés UAE (écran HR, My leave, planning)

**Files:**
- Modify: `hr.js` (`renderHRManager`, `renderHRMine` ; handlers `hrSaveHolidayUI`, `hrDeleteHolidayUI`)
- Modify: `app.js` (stockage `publicHolidays` vers la ligne 1250, bandeau day-header vers la ligne 3772)

**Interfaces:**
- Consumes: `hrData.holidays` (Task 3), `hrUpcomingHolidays` / `hrHolidayNameOn` (Task 2), routes `hrSaveHoliday` / `hrDeleteHoliday` (Task 3), `allRes.holidays` de getAllData.
- Produces: globale `publicHolidays` (app.js) alimentée par getAllData ; badge férié dans l'en-tête de jour du planning.

- [ ] **Step 1: Section fériés écran HR manager**

Dans `renderHRManager`, juste avant la section « Team » :

```js
  const holidays = hrUpcomingHolidays(hrData.holidays || [], today, 12);
  h += '<div class="hr-section"><h3>Public holidays</h3>';
  holidays.forEach(hd => {
    h += '<div class="hr-card"><div class="hr-row"><div class="hr-grow">' +
      '<div class="hr-name">' + esc(hd.name) + '</div>' +
      '<div class="hr-meta">' + esc(hd.holiday_date) + '</div></div>' +
      '<button class="hr-btn-alt" style="padding:6px 10px;border:none;border-radius:8px;cursor:pointer" data-action="hrDeleteHolidayUI" data-arg0="' + hd.id + '" title="Delete" aria-label="Delete holiday">' + icon('trash', 14) + '</button>' +
      '</div></div>';
  });
  if (!holidays.length) h += '<div class="hr-empty">No upcoming public holiday on file.</div>';
  h += '<div class="hr-card"><div class="hr-form">' +
    '<label>Date</label><input type="date" id="hrHolidayDate"/>' +
    '<label>Name</label><input id="hrHolidayName" placeholder="Eid Al Fitr"/>' +
    '</div><div class="hr-actions">' +
    '<button class="hr-btn-ok" data-action="hrSaveHolidayUI">Add holiday</button>' +
    '</div></div></div>';
```

- [ ] **Step 2: Handlers**

```js
// CRUD feries (manager). L'upsert serveur ecrase le nom si la date existe deja.
async function hrSaveHolidayUI(){
  if (hrSubmitting) return;
  const date = hrVal('hrHolidayDate'), name = hrVal('hrHolidayName');
  if (!date || !name) { toast('Pick a date and a name', 'error'); return; }
  hrSubmitting = true;
  try {
    await apiWrite('hrSaveHoliday', { body: { holiday_date: date, name: name } });
    toast('Holiday saved', 'success');
    hrData = null;
    await loadHR();
  } catch (e) {
    toast((e && e.message) || 'Failed to save', 'error');
  } finally {
    hrSubmitting = false;
  }
}

async function hrDeleteHolidayUI(id){
  if (!confirm('Delete this public holiday?')) return;
  try {
    await apiWrite('hrDeleteHoliday', { body: { id: Number(id) } });
    toast('Holiday deleted', 'info');
    hrData = null;
    await loadHR();
  } catch (e) {
    toast((e && e.message) || 'Failed to delete', 'error');
  }
}
```

- [ ] **Step 3: Fériés côté employé**

Dans `renderHRMine`, après le bloc des soldes :

```js
  const nextHolidays = hrUpcomingHolidays(hrData.holidays || [], hrToday(), 4);
  if (nextHolidays.length) {
    h += '<div class="hr-section"><h3>Upcoming public holidays</h3><div class="hr-card">' +
      nextHolidays.map(hd => '<div class="hr-stat"><span>' + esc(hd.name) + '</span><b>' + esc(hd.holiday_date) + '</b></div>').join('') +
      '</div></div>';
  }
```

- [ ] **Step 4: Planning, stockage + bandeau**

Dans `app.js`, près de la ligne 1250 (`if(typeof hrSetApprovedLeaves==='function') hrSetApprovedLeaves(allRes.leaves||[]);`), ajouter une globale et son alimentation :

```js
    publicHolidays = allRes.holidays || [];
```

Déclarer `let publicHolidays=[];` à côté des autres globales de données (chercher `let cleaners=` ou équivalent et déclarer au même endroit).

Dans le rendu du day-header (app.js:3772), après `formatDate(date)` :

```js
    const __holiday=(typeof hrHolidayNameOn==='function')?hrHolidayNameOn(publicHolidays,date):null;
```

et dans la chaîne HTML du header, juste après le `</span>` du `day-title` :

```js
      (__holiday?'<span class="hr-badge onleave" style="margin-left:8px">🎉 '+esc(__holiday)+'</span>':'')+
```

- [ ] **Step 5: Tests + vérification manuelle**

Relancer `tests/hr.spec.ts`. Puis serveur local : vérifier visuellement que le planning se rend sans erreur console (les fériés n'arrivent qu'avec un vrai payload getAllData ; l'absence de données ne doit rien casser : `publicHolidays` vaut `[]`).

- [ ] **Step 6: Commit**

```bash
git add hr.js app.js
git commit -m "feat(rh): feries UAE dans HR, My leave et bandeau planning"
```

---

### Task 10: Déploiement + vérification de bout en bout (SESSION PRINCIPALE)

**Files:**
- Modify: `sw.js` (stampé par le script de déploiement)

Cette tâche N'EST PAS déléguée à un subagent : déploiement prod + comptes réels.

- [ ] **Step 1: Confirmer avec Hillal avant tout déploiement**

- [ ] **Step 2: Déployer l'edge function**

```bash
cd "/Users/hillal/Documents/Wix new/hk-planner-repo" && ./deploy-proxy.sh
```

- [ ] **Step 3: Déployer le front**

```bash
./deploy-front.sh
# puis commiter le sw.js stampe comme le rappelle le script
git add sw.js && git commit -m "chore: stamp sw VERSION apres deploiement HR v2"
```

- [ ] **Step 4: Vérification de bout en bout (prod, comptes réels)**

1. Login manager : l'écran HR montre Pending, Public holidays (seed visible), Team.
2. Dossier Semax : « Leave history » montre le congé approuvé qui avait « disparu » (decided_by, dates, badge approved).
3. « Download form » sur ce congé : PDF avec « Approved in app by [nom] » (génération paresseuse legacy).
4. Login employé (ou soumission test) : Request leave → pad de signature → soumission OK. Toast + notif Telegram manager.
5. Manager : Approve → pad de signature → décision OK. « Download form » : PDF avec les DEUX signatures.
6. Annuler le congé test (Cancel this leave) pour ne pas polluer les soldes.
7. Planning : bandeau férié visible sur un jour férié (ex. naviguer vers début décembre 2026).
8. Ajouter puis supprimer un férié de test dans Public holidays.

- [ ] **Step 5: Commit final éventuel + clôture**

## Self-review du plan (fait à l'écriture)

- Couverture spec : historique (T2, T3, T8), signatures (T1, T4, T6, T7), PDF + Storage + legacy (T1, T5, T8), fériés affichage + planning (T1, T3, T9), sécurité (T4 validation, T5 auth own/manager, URL signée 60 s), tests (T2), déploiement (T10). Décompte des congés inchangé : aucun code n'y touche.
- Cohérence des noms entre tâches vérifiée : `hrValidSignature`, `hrArchiveLeaveForm`, `hrBuildLeaveFormPdf`, `hrSplitHistory`, `hrUpcomingHolidays`, `hrHolidayNameOn`, `hrOpenSignaturePad`, `hrSendDecision`, `hrDownloadForm`, `hrSaveHolidayUI`, `hrDeleteHolidayUI`, `publicHolidays`, routes `hrSaveHoliday` / `hrDeleteHoliday` / `hrLeaveForm`.
- Points d'attention pour l'implémenteur : la lecture des query params dans `hrLeaveForm` doit copier le mécanisme des routes GET existantes ; la table des routes (ligne ~740) doit être comprise avant d'y ajouter les 3 entrées ; si la délégation `data-action` est une table blanche, y ajouter les nouveaux handlers.


